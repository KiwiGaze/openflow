{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "IPC capabilities for the OpenFlow settings window and overlays.",
  "windows": ["main", "hud", "changes"],
  "permissions": ["core:default"]
}
